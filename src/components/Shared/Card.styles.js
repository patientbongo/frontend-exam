import { colors } from "./sharedStyles";

export const styles = {
  card: {
    borderRadius: "3px",
    boxShadow: "0 1px 2px rgba(0, 0, 0, 0.2)",
    margin: "7px",
    backgroundColor: colors.white,
    // padding: "10px",
  },
};
